// URL base de la API. Para producción, cambia a tu API (ej. https://api.tudominio.com/api).
// Debe coincidir con host_permissions en manifest.json.
window.VAULT_API_URL = "http://localhost:8000/api";
